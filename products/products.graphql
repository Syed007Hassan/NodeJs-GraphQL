type Query {
  products: [Product]
}

type Product {
  id: ID!
  description: String!
  reviews: [Review]
  price: Float!
  category: String!
  orders: [Order]
}

type Review {
  rating: Int!
  comment: String!
}
