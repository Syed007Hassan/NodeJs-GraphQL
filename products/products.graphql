type Query {
  products: [Product]
  orders: [Order]
}

type Product {
  id: ID!
  description: String!
  reviews: [Review]
  price: Float!
  category: String!
  orders: [Order]
}

type Review {
  rating: Int!
  comment: String!
}
